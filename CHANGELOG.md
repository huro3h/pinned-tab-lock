# Changelog

このプロジェクトの主な変更点を記録します。

フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョニングは [セマンティック バージョニング](https://semver.org/lang/ja/) を採用しています。

新しいバージョンを上に追記します。未リリースの変更は `[Unreleased]` にためていき、
リリース時にバージョン番号と日付へ繰り上げます。

## [Unreleased]

## [1.0.0] - 2026-08-29

### Added

- 初回リリース。以下の機能を含みます。
- **固定タブの上書き防止**: 固定タブで別サイトを開こうとすると、そのURLを新しいタブで開き直し、固定タブは元のページに戻す。ブックマークのクリックでもアドレスバーへの入力でも同じように働く。
- **守る対象の自動決定**: サイトを事前に登録する必要はなく、固定した時点でそのタブが開いていたサイトがそのまま持ち場になる。固定を解除すれば制限も消える。
- **スキームを問わない保護**: 通常のウェブページに加えて、`chrome-extension://` のページ(自作の新しいタブ拡張など)、`file://` のローカルファイル、`chrome://` の内部ページを固定した場合も保護する。同一サイトの判定は「スキーム + ホスト」の完全一致。
- **サーバーリダイレクトへの対応**: 同一ホストのURLが別ホストへリダイレクトする場合も検知して新しいタブへ逃がす。
- **タブをアクティブで開く**: 逃がした先のタブにすぐ切り替えるか、バックグラウンドで開いて固定タブを見たままにするかを選べる(既定オン)。
- **新しいタブページも保護する**: ブラウザの新しいタブページを固定したときも保護対象にする(既定オフ)。メモや時計を出す新しいタブを固定したままにしたいとき用。
- **ツールバーアイコンで状態確認**: アイコンの色(青＝保護オン/グレー＝オフ)とツールチップで現在の状態を可視化。
- **保護のON/OFF**: 拡張を読み込んだまま一時的に無効化できる。
